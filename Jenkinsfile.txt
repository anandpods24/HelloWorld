pipeline {
    
    agent any
    stages {
        stage ('Checkout from SCM') {
            steps {
                echo 'Checkout Successful'
                checkout([$class: 'GitSCM', branches: [[name: '*/master']], doGenerateSubmoduleConfigurations: false, extensions: [], submoduleCfg: [], userRemoteConfigs: [[credentialsId: '0420115f-e0ea-4993-9139-aeaec37931af', url: 'https://github.com/anandpods24/HelloWorld.git']]])
            }
            
        }
        stage ('Unit Testing') {
            steps {
             echo 'Unit Testing Successful'   
            }
        }
        stage ('Compile and deploy') {
            steps {
            echo 'Compilation and Deploy successful'
            }
        }
    }
}